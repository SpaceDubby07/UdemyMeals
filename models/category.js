// What should a category object look like? -- this is a class
class Category {
    constructor(id, title, color) {
        this.id = id;
        this.title = title;
        this.color = color;
    }
}

export default Category;